import { createStore } from 'vue'
import auth from './auth' 
import Vuex from 'vuex'
import Vue from 'vue'
Vue.use(Vuex)

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
     auth
  }
})
